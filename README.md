# CURVE Navigation

ドライブとワインディング探索のためのナビゲーションアプリです。

## Development

```bash
npm install
npm run dev
```

フロントエンドは `apps/web`、将来のAPIは `apps/api`、共有契約は `packages/contracts` に配置します。詳細は [architecture](docs/architecture.md) を参照してください。
