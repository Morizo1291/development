version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    volumes:
      - .:/workspace
    tty: true
    stdin_open: true
    ports:
      - "3000:3000"
      - "5173:5173"
      - "8080:8080"
    command: bash
